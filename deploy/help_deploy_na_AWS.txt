##### Execução na AWS #####

# Crie sua conta gratuita na AWS.
# Crie uma instância EC2 da camada gratuita AWS.
# Acesse a instância pelo terminal conforme mostrado nas aulas.
# Crie uma pasta 'Download' e faça o download do Miniconda (Anaconda é muito pesado para a instância EC2 gratuita):

mkdir Download
cd Download
wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh

# Instale o miniconda:
bash Miniconda3-latest-Linux-x86_64.sh

# Volte para o diretório raiz e atualize o '.bash_profile' (para não precisar reiniciar):
cd ..
source .bash_profile


# Baixe o arquivo zip contendo o projeto (p. ex. do seu dropbox), renomeie-o para 'algumacoisa.zip' e descompacte-o:
wget https://uc58081824b369ce79e8a5dc9be4.dl.dropboxusercontent.com/cd/0/get/C835....
mv antigonome algumacoisa.zip
unzip algumacoisa.zip

# Execute os comandos abaixo:
cd PNAD-COVID-streamlit
conda create -n pnad-covid python=3.11
conda activate pnad-covid
pip install -r requirements.txt 

cd deploy
streamlit run deploy.py

< control + C > # Para Cancelar

nohup streamlit run deploy.py --server.port=8501 --server.address=0.0.0.0 &




# Acessando:
ec2-18-118-30-227.us-east-2.compute.amazonaws.com:8501